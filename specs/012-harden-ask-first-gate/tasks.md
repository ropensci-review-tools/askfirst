---
created: 2026-07-27T16:05:20Z
agent: claude-sonnet-5
git_hash: 3018ad2b681f5cc73f156f793b0d3632491d1f2a
---

# Tasks: harden-ask-first-gate

## T012-1: Halt `askfirst_check_scenarios()` at high confidence
- [ ] T012-1: In `bindings/r/R/scenarios.R`, change the `askfirst_signal("askfirst_scenario_check", pkg = pkg, message = message_text)` call inside `askfirst_check_scenarios()`'s high-confidence branch to pass `call_stop = TRUE`, and attach the scenarios vector as a named condition field (e.g. `scenarios = scenarios`), matching how `askfirst_capability_gap()` attaches its own fields via `askfirst_signal()`. Since the call no longer returns normally at high confidence, remove or restructure the trailing `invisible(scenarios)` return so it's only reached on the low/medium-confidence paths (the halting path never gets there).

## T012-2: Update `askfirst_check_scenarios()` roxygen docs for the new halting behavior
- [ ] T012-2: In `bindings/r/R/scenarios.R`, rewrite the `@return` roxygen block for `askfirst_check_scenarios()`: remove the claim that it "also signals a non-fatal `askfirst_scenario_check` condition" at high confidence and returns the scenario vector regardless of confidence. Describe the high-confidence path as halting (mirroring the phrasing already used in `askfirst_capability_gap()`'s docs in `bindings/r/R/capability_gap.R`: "Unlike the load-time notice or error-time redirect, halting is the deliberate intent here"), while keeping the existing description of the low/medium-confidence plain-return behavior unchanged.

## T012-3: Rewrite the affected `test-scenarios.R` high-confidence test for halting semantics
- [ ] T012-3: In `bindings/r/tests/testthat/test-scenarios.R`, rewrite `test_that("askfirst_check_scenarios signals askfirst_scenario_check at high confidence", ...)` and `test_that("askfirst_check_scenarios auto-loads namespace for unregistered package", ...)` (both currently use `withCallingHandlers()` + `invokeRestart("muffleMessage")` and assert a return value) to instead use `tryCatch(..., askfirst_scenario_check = function(cnd) cnd)` and assert on the caught condition object (class `askfirst_scenario_check`/`askfirst_condition`/`error`, `pkg` field, message content, and the new `scenarios` condition field), matching the pattern in `bindings/r/tests/testthat/test-capability-gap.R`'s `"askfirst_capability_gap halts with a askfirst_capability_gap condition under high confidence"` test. Leave the medium-confidence, low-confidence, and unregistered-package-error tests unchanged — those paths are unaffected.

## T012-4: Add severity-based `directive:` line to `askfirst_signal()`
- [ ] T012-4: In `bindings/r/R/conditions.R`, replace the single hardcoded `directive_line <- "directive: ask-before-proceeding"` in `askfirst_signal()` with a lookup keyed by `class` (reusing or extending the existing `type_map` structure): `askfirst_capability_gap`, `askfirst_scenario_check`, and `askfirst_error_redirect` map to `"directive: stop-and-ask"`; `askfirst_notice` maps to `"directive: notice"`.

## T012-5: Update `askfirst_signal()` roxygen docs for the differentiated directive line
- [ ] T012-5: In `bindings/r/R/conditions.R`, update the roxygen block above `askfirst_signal()` (the paragraph describing `prefix = TRUE` behavior, currently stating a single uniform `directive: ask-before-proceeding` line) to describe the two possible values (`stop-and-ask` vs `notice`) and which of the four signal classes gets which. Also update the one-line description of `"askfirst_error_redirect"` in the same docblock (currently just "non-fatal") to note it always accompanies an already-halting error from the adopting package's own code, which is why it now carries `directive: stop-and-ask` despite `askfirst_signal()` itself using `rlang::inform()` for it.

## T012-6: Update tests asserting the old uniform directive text
- [ ] T012-6: In `bindings/r/tests/testthat/test-init.R`, update `expect_match(msg, "directive: ask-before-proceeding", fixed = TRUE)` (appears twice: once implicitly via the notice test around line 179, and check for any other occurrence) to `expect_match(msg, "directive: notice", fixed = TRUE)`, since the tests in that file signal via `askfirst_notice`. Search the full `bindings/r/tests/` directory for any other hardcoded `"ask-before-proceeding"` string (e.g. in capability_gap or scenario tests) and update each to the correct new value (`"directive: stop-and-ask"` for capability_gap/scenario_check/error_redirect tests, `"directive: notice"` for notice tests).

## T012-7: Rework hook rule 6 (and adjust rule 5) in the canonical `agent-hooks/claude/session_start.sh`
- [ ] T012-7: In `agent-hooks/claude/session_start.sh`, rewrite rule 6 of the `<askfirst-context>` block. Replace the current text (which instructs marking "ask the user" as a recommended option within a two-option menu) with an instruction that when a `directive: stop-and-ask` signal fires (from `askfirst_check_scenarios()`, `askfirst_capability_gap()`, or `askfirst_error_redirect`), the agent's only immediate next step is to surface the upstream question to the user and wait for their answer — it must not present, draft, or offer to implement a workaround as a selectable option in the same turn. Adjust rule 5 if needed so it reads coherently alongside the new rule 6 (rule 5's non-exhaustive-scenario-list point stays; rule 6 should reference the `directive: stop-and-ask` vs `directive: notice` distinction from T012-4 so the agent knows which signals gate further code-writing on that topic and which don't). Update the numbered list in the block's intro (currently documents 4 signal types by name) if the rule renumbering affects it.

## T012-8: Mirror the rule 6 rewrite in `agent-hooks/opencode/session_start.sh`
- [ ] T012-8: Apply the identical edit from T012-7 to `agent-hooks/opencode/session_start.sh`. After editing, run `diff agent-hooks/claude/session_start.sh agent-hooks/opencode/session_start.sh` to confirm the two files are still byte-identical, as they were before this stage.

## T012-9: Add `tools/generate-install-hooks.sh` to regenerate the installer's embedded hook content from `agent-hooks/`
- [ ] T012-9: Create `tools/generate-install-hooks.sh`, a script that reads `agent-hooks/claude/session_start.sh` and `agent-hooks/claude/post_tool_use.sh` (canonical source; `claude/` and `opencode/` are byte-identical) and rewrites the two heredoc blocks inside `tools/install-agent-hooks.sh`'s `write_session_start()` (the `<<'SESSION_HOOK' ... SESSION_HOOK` block, specifically the inner `<<'ASKFIRST_CONTEXT' ... ASKFIRST_CONTEXT` portion sourced from `session_start.sh`'s `<askfirst-context>...</askfirst-context>` body) and `write_post_tool_use()` (the `<<'POST_HOOK' ... POST_HOOK` block, sourced from `post_tool_use.sh`'s `main()` body) in place, without touching the surrounding installer logic (arg parsing, tool detection, settings.json registration). The script must not assume anything binding-specific — it only ever reads from `agent-hooks/` and writes to `tools/install-agent-hooks.sh`.

## T012-10: Document the generation requirement and run it to sync current content
- [ ] T012-10: Add a comment at the top of `tools/install-agent-hooks.sh` (alongside the existing header comment) noting that the embedded `session_start.sh`/`post_tool_use.sh` content is generated from `agent-hooks/claude/*.sh` via `tools/generate-install-hooks.sh`, and must be regenerated after any edit to those canonical files. Run `tools/generate-install-hooks.sh` now so the installer's embedded copy picks up rules 5 and 6 from stage 011 plus the T012-7/T012-8 rewrite (currently the installer is missing both). Since `bindings/r/inst/install-agent-hooks.sh` is a symlink to `tools/install-agent-hooks.sh`, no separate edit is needed there.

## T012-11: Add a drift regression test between the installer and canonical `agent-hooks/`
- [ ] T012-11: Add a test that fails if `tools/install-agent-hooks.sh`'s embedded heredoc content ever diverges from `agent-hooks/claude/session_start.sh` / `agent-hooks/claude/post_tool_use.sh` again. Implement as a testthat test (e.g. a new `bindings/r/tests/testthat/test-install-agent-hooks.R`) that extracts the `<askfirst-context>...</askfirst-context>` block from both `tools/install-agent-hooks.sh`'s heredoc and `agent-hooks/claude/session_start.sh` directly, and asserts they're identical (and similarly for the `post_tool_use.sh` `main()` body), using `here::here()` or an equivalent repo-root-relative path resolution so the test works regardless of working directory. This is a repo-structure check, not an R-package-behavior check, so keep the test resilient to running outside a full package install (skip gracefully if the repo-root files can't be located, e.g. when tests run from an installed/tarball copy without the top-level `agent-hooks/`/`tools/` directories present).

## T012-12: Update `using-askfirst.Rmd` vignette for scenario_check's new halting behavior
- [ ] T012-12: In `bindings/r/vignettes/using-askfirst.Rmd`, section "3. What an agent sees: `askfirst_check_scenarios()`": replace the sentence stating it "signals a non-fatal ... condition" at `"high"`/`"medium"` confidence with text describing the halt, and correct the confidence-level claim to `"high"` only (the code only signals at high confidence per stage 010 — `"medium"` was already an inaccuracy predating this stage). In section "4. What your end users experience", add a clause describing `askfirst_check_scenarios()`'s halting behavior when an agent calls it on its own initiative, parallel to the existing sentence describing `askfirst_capability_gap()`'s halt.

## T012-13: Regenerate `man/` pages with roxygen2
- [ ] T012-13: From `bindings/r/`, run `roxygen2::roxygenise()` (or `devtools::document()`) to regenerate `man/` pages from the roxygen docblocks touched in T012-2 and T012-5 (`askfirst_check_scenarios()` and `askfirst_signal()`), and confirm the updated `.Rd` files are staged along with the source changes.

## T012-14: Full verification pass
- [ ] T012-14: Run the R package test suite (`devtools::test()` or `R CMD check` from `bindings/r/`) to confirm all tests pass after T012-1 through T012-6, T012-11, and T012-13. Manually run `tools/install-agent-hooks.sh --tool claude --overwrite` against a scratch directory with a `.claude/settings.json` present, and inspect the generated `session_start.sh` to confirm it now contains the full 6-rule (or renumbered equivalent) block matching `agent-hooks/claude/session_start.sh` exactly.
