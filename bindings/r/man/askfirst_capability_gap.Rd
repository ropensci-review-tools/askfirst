% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capability_gap.R
\name{askfirst_capability_gap}
\alias{askfirst_capability_gap}
\title{Flag a capability gap to a potential LLM/AI-agent caller}
\usage{
askfirst_capability_gap(pkg, message)
}
\arguments{
\item{pkg}{The name of the calling package (a single string). Passed
explicitly, not auto-detected, so the emitted condition is
attributable to a specific package even when several packages have
adopted \code{askfirst} in the same session.}

\item{message}{Message text describing the capability gap, using
cli/glue-style \code{{}} interpolation.}
}
\value{
Invisibly, \code{NULL}, for human/low-confidence sessions. Does not
return otherwise (halts via \code{rlang::abort()}).
}
\description{
Call this inline, from your own package's code, at the exact point you
recognize a known limitation or unimplemented capability has been hit —
a case where nothing errors (the call otherwise "succeeds") but does not
meet the caller's actual need. See
\code{specs/002-design-agnostic-spec/design.md} (T002-4/T002-5) for why this
requires explicit author instrumentation rather than mechanical
detection.
}
\details{
A no-op for human/low-confidence sessions (per the "no false positives
for humans" constraint). For \code{"high"} confidence sessions,
halts execution with a \code{askfirst_capability_gap} condition attributed to
\code{pkg}, since — unlike the load-time notice or error-time redirect —
halting is the deliberate intent here.
}
\examples{
\dontrun{
# Inside an adopting package's own function body, at a known-limitation
# branch:
my_function <- function(x, grouped = FALSE) {
  if (grouped) {
    askfirst::askfirst_capability_gap(
      "mypackage",
      "grouped input is not yet supported; falls back to ungrouped behavior"
    )
  }
  # ... rest of the function
}
}
}
