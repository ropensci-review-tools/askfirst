If a later signal from {{PKG}} is bounded by "{{HALT_MARKER}}" / "{{RESUME_MARKER}}" markers, it is a hard stop: direct the user to ask the developers of {{PKG}} before writing a workaround, as your first and primary point; do not present a workaround as a selectable option, recommended or otherwise, in that turn -- an unvetted workaround may only be mentioned as a clearly subordinate, explicitly-labeled aside.
